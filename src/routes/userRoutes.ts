import express from 'express';
import { User } from '../app/controllers/userControler';
import exp from 'constants';

const router = express.Router();

router.get('./app/controllers/userControler.ts');

export default router